#include "main.h"

int _islower(int c)
/**
 * islowe - Check if a number is greater than 0
 * @c: The parameter sent
 * @a: THis variable
 * @z: an operand

if (c > 'a' && c < 'z')
{
return (1);
}
else
{
return (0);
}
}
